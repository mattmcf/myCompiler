
	nop
	nop # (quad -1) -- initialization
	nop
	irmovl fffb, %esp
	rrmovl %esp, %ebp
	irmovl $4, %eax
	subl %eax, %esp

	nop
	nop # (quad 0) -- function prolog
	nop
	pushl %ebp
	rrmovl %esp, %ebp
	irmovl $-20, %eax
	subl %eax, %esp	mrmovl -8(%ebp), %eax
	mrmovl -12(%ebp), %ebx
	addl %ebx, %eax
	rmmovl %eax, -20(%ebp)
	mrmovl -8(%ebp), %eax

	nop
	nop # (quad 10) -- function epilog
	nop
	rrmovl %ebp, %esp
	popl %ebp

	nop
	nop # (quad -1) -- done
	nop
	halt

